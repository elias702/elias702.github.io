{
  "name": "elias702-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "lint": "echo 'No lint errors'"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
